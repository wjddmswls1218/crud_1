<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body
        h1 Hello POST

        if(status)
            each data in list 
                h2 #{data.title}
                h4 #{data.description}
                hr
        else    
            h1 We Failed Call Data in Database